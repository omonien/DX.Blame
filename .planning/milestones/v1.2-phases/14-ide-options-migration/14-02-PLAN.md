---
phase: 14-ide-options-migration
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/DX.Blame.Registration.pas
autonomous: true
requirements: [SETT-03]

must_haves:
  truths:
    - "Tools menu no longer has a 'DX Blame' submenu with 'Enable Blame' and 'Settings...' items"
    - "The Ctrl+Alt+B keyboard shortcut still toggles blame (handled by KeyBinding.pas, unaffected)"
    - "The editor context menu 'Enable/Disable Blame' toggle still works (handled by Navigation.pas, unaffected)"
    - "IDE does not crash on startup, shutdown, or BPL unload"
  artifacts:
    - path: "src/DX.Blame.Registration.pas"
      provides: "Registration without Tools menu creation, SyncEnableBlameCheckmark as no-op"
      contains: "SyncEnableBlameCheckmark"
  key_links:
    - from: "src/DX.Blame.KeyBinding.pas"
      to: "src/DX.Blame.Registration.pas"
      via: "OnBlameToggled callback assigned to SyncEnableBlameCheckmark (still compiled, now no-op)"
      pattern: "OnBlameToggled.*SyncEnableBlameCheckmark"
    - from: "src/DX.Blame.Navigation.pas"
      to: "src/DX.Blame.Registration.pas"
      via: "GOnContextMenuToggle callback assigned to SyncEnableBlameCheckmark (still compiled, now no-op)"
      pattern: "GOnContextMenuToggle.*SyncEnableBlameCheckmark"
---

<objective>
Remove the legacy Tools > DX Blame menu and all associated code from Registration.pas. The IDE Options page (Plan 01) and the context menu toggle (Phase 13) replace all menu functionality.

Purpose: Clean up the deprecated UI surface. SETT-03 requires no Tools menu items remain after migration. The context menu toggle (Ctrl+Alt+B) and IDE Options page are the replacement access points.
Output: Registration.pas without menu creation code. SyncEnableBlameCheckmark remains as a no-op stub because KeyBinding.pas and Navigation.pas reference it via callbacks.
</objective>

<execution_context>
@C:/Users/Test/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ide-options-migration/14-RESEARCH.md
@.planning/phases/14-ide-options-migration/14-01-SUMMARY.md

@src/DX.Blame.Registration.pas

<interfaces>
<!-- Contracts that must remain intact after menu removal -->

From src/DX.Blame.Registration.pas (interface section, must remain):
```pascal
procedure Register;
procedure SyncEnableBlameCheckmark;
```

From src/DX.Blame.KeyBinding.pas (assigns callback):
```pascal
var OnBlameToggled: TProc;
// Assigned in Registration.pas Register: OnBlameToggled := SyncEnableBlameCheckmark;
```

From src/DX.Blame.Navigation.pas (assigns callback):
```pascal
var GOnContextMenuToggle: TProc;
// Assigned in Registration.pas Register: GOnContextMenuToggle := SyncEnableBlameCheckmark;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Tools menu code from Registration.pas</name>
  <files>src/DX.Blame.Registration.pas</files>
  <action>
Remove the following from DX.Blame.Registration.pas:

**Remove from implementation uses clause:**
- `Vcl.Menus` (no longer needed — was for TMenuItem)
- `DX.Blame.Settings.Form` (no longer needed — ShowSettings call removed)

**Remove unit-level vars:**
- `GMenuParentItem: TMenuItem = nil;`
- `GEnableBlameItem: TMenuItem = nil;`
- `GMenuHandler: TObject = nil;`

**Remove TDXBlameMenuHandler class entirely:**
- The class declaration (type section)
- Both method implementations: ToggleBlame and ShowSettings

**Remove CreateToolsMenu procedure entirely:**
- The entire procedure body (lines 127-189 approximately)

**Remove RemoveToolsMenu procedure entirely:**
- The entire procedure body (lines 195-200 approximately)

**Update Register procedure:**
- Remove the `CreateToolsMenu;` call (was between about box registration and RegisterIDENotifiers)

**Update SyncEnableBlameCheckmark:**
Replace the body with a comment explaining the Tools menu was removed. The procedure MUST remain in the interface section because KeyBinding.pas and Navigation.pas assign it to callback variables. New body:
```pascal
procedure SyncEnableBlameCheckmark;
begin
  // Tools menu removed in v1.2 Phase 14 -- no-op, kept for callback contract
  // (OnBlameToggled in KeyBinding.pas, GOnContextMenuToggle in Navigation.pas)
end;
```

**Update finalization:**
- Remove the `RemoveToolsMenu;` call (step 6 in the finalization sequence)
- Add a comment noting the step was removed:
```pascal
// 6. (Tools menu removed in v1.2 — no cleanup needed)
```

**Do NOT remove:**
- The GAddInOptions registration/unregistration added in Plan 01
- The GStatusbar var and its lifecycle code
- Any other existing code not related to the Tools menu

**Do NOT remove the SyncEnableBlameCheckmark procedure declaration from the interface section.** It remains as a public no-op. Both OnBlameToggled and GOnContextMenuToggle callback assignments in Register remain unchanged — they still point to SyncEnableBlameCheckmark, which is now a no-op. This is correct and expected. The callbacks previously synced a checkmark on a menu item that no longer exists; no other side effect is needed because the context menu toggle in Navigation.pas already handles its own checkmark independently.
  </action>
  <verify>
    <automated>powershell -Command "& './build/DelphiBuildDPROJ.ps1' -Project './src/DX.Blame.dproj'"</automated>
  </verify>
  <done>
- Package compiles with zero errors
- No TDXBlameMenuHandler class in Registration.pas
- No CreateToolsMenu or RemoveToolsMenu procedures in Registration.pas
- No GMenuParentItem, GEnableBlameItem, GMenuHandler vars in Registration.pas
- No `Vcl.Menus` or `DX.Blame.Settings.Form` in Registration.pas implementation uses
- SyncEnableBlameCheckmark remains in interface section as no-op stub
- Register procedure does not call CreateToolsMenu
- Finalization does not call RemoveToolsMenu
- OnBlameToggled and GOnContextMenuToggle callback assignments unchanged
  </done>
</task>

</tasks>

<verification>
1. `powershell -Command "& './build/DelphiBuildDPROJ.ps1' -Project './src/DX.Blame.dproj'"` compiles with 0 errors
2. Registration.pas has no reference to TMenuItem, TDXBlameMenuHandler, CreateToolsMenu, or RemoveToolsMenu
3. SyncEnableBlameCheckmark is still declared in interface section and has a no-op body
4. The two callback assignments (OnBlameToggled, GOnContextMenuToggle) remain in Register procedure
</verification>

<success_criteria>
- Package compiles successfully after menu removal
- All menu-related code removed (class, procedures, vars, uses)
- SyncEnableBlameCheckmark preserved as no-op for callback contract compatibility
- No regression in keyboard shortcut (Ctrl+Alt+B) or context menu toggle functionality
- Finalization sequence remains correct (AddInOptions unregister before wizard removal)
</success_criteria>

<output>
After completion, create `.planning/phases/14-ide-options-migration/14-02-SUMMARY.md`
</output>
